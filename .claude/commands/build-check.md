Run `npx next build` and verify:
1. All 14 routes generate successfully
2. No TypeScript errors
3. No missing imports
4. First load JS shared bundle stays under 90KB
5. Individual page sizes stay under 170KB first load

If build fails, diagnose and fix the issue before reporting.
